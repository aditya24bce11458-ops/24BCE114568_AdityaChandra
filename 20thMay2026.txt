use vitcampus;
CREATE TABLE Persons (
    ID int NOT NULL,
    LastName varchar(255) unique not null,
    FirstName varchar(255) NOT NULL,
    Age int,
    CHECK (Age>=18),
    city varchar(255) default 'Coimbitore'
);
desc persons;

insert into persons values (101,'V','Jayanth',null);
select * from persons;

ALTER TABLE Persons
ADD UNIQUE (AGE);

ALTER TABLE Persons
DROP CONSTRAINT Age;

ALTER TABLE Persons	
ADD CHECK (Age>=18);

drop table persons;





create table products(
    pid int primary key,
    pname varchar(100) not null,
    prefNO int not null unique
);

desc products;

insert into products values (101,'electronics',19929);

select * from products;

alter table products
drop primary key;

alter table products
add primary key(pid);

create table category(
    cid int primary key,
    cname varchar(100) not null
 );
 
 insert into category values (101,'electronics'),(102,'Home App');
 
 create table cproducts(
    pid int primary key,
    pname varchar(100) not null,
    cid int,
    foreign key (cid) references category(cid)
);

insert into cproducts values (501, 'HP Laptop', 101);

insert into cproducts values (502, 'Wooden Chair', 102);

insert into cproducts values (503, 'iPhone', 103);

insert into cproducts values (504,'Power Bank Samsung',64-bit encry key);

delete from category where cid=101;

select * from products;